"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var common_1 = require("./common");
;
;
function getMongoDbSort(sort, path) {
    if (path === void 0) { path = []; }
    return Object.assign.apply(Object, [{}].concat(Object.keys(sort)
        .filter(function (key) { return key != common_1.FICTIVE_SORT; })
        .map(function (key) {
        var _a;
        var value = sort[key];
        var newPath = path.concat([key]);
        if (typeof value === 'number') {
            return _a = {}, _a[newPath.join(".")] = value, _a;
        }
        return getMongoDbSort(value, newPath);
    })));
}
exports.default = getMongoDbSort;
